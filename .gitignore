# Claude Code project instructions - local-only, never pushed.
CLAUDE.md

data/settings.json
data/collections.json
data/cards.json
data/boosters.json
data/app-log.json
data/pending-state.json
logs/
node_modules/

# Local test sandboxes now live outside this repo (C:\Users\marco\Documents\CardPackWidget-TestApp
# and CardPackWidget-Windows, since 2026-07-27), so they no longer need entries here. Build output
# below still lives inside the repo and contains live data/tokens - not meant to be version controlled.
dist/
bin/
CardPackWidget-Windows.zip
CardPackWidget-v*.zip
UpdateTest-v1.0.0/
UpdateTest-*/
InstallTest-*/
